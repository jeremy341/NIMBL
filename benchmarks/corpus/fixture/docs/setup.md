# Setup

To set up the runtime, choose the theme in `src/config.ts` and tune the maximum number of retries. The report generator lives in `src/features/report.ts`.

```bash
bun install
bun run nimbl
```
